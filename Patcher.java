import java.nio.file.Files; import java.nio.file.Paths; public class Patcher { public static void main(String[] args) throws Exception { String path = "common/src/main/java/com/luminabox/client/gui/MusicPlayerScreen.java"; String content = new String(Files.readAllBytes(Paths.get(path)), "UTF-8"); content = content.replace("private Button stopButton;", "private Button stopButton;\n    private net.minecraft.client.gui.components.Button modeButton;"); String helper = "\n    private String getModeIcon(com.luminabox.audio.CustomMusicManager.PlaybackMode mode) {\n        switch (mode) {\n            case LOOP_ALL: return \"\uD83D\uDD01\";\n            case LOOP_ONE: return \"\uD83D\uDD02\";\n            case SHUFFLE: return \"\uD83D\uDD00\";\n            case SEQUENTIAL: default: return \"\u2B07\";\n        }\n    }\n"; content = content.replace("    private void toggleSelectMode() {", helper + "    private void toggleSelectMode() {"); String modeBtn = "\n        modeButton = this.addRenderableWidget(net.minecraft.client.gui.components.Button.builder(\n            net.minecraft.network.chat.Component.literal(getModeIcon(com.luminabox.audio.CustomMusicManager.getInstance().getPlaybackMode())),\n            button -> {\n                com.luminabox.audio.CustomMusicManager.getInstance().cyclePlaybackMode();\n                button.setMessage(net.minecraft.network.chat.Component.literal(getModeIcon(com.luminabox.audio.CustomMusicManager.getInstance().getPlaybackMode())));\n            }\n        ).bounds(cardX + 365, cardY + 270, 20, 20).build());\n"; content = content.replace("        // --- TAB 0: LOCAL TAB WIDGETS ---", modeBtn + "        // --- TAB 0: LOCAL TAB WIDGETS ---"); int start = content.indexOf("importButton = this.addRenderableWidget(Button.builder("); int end = content.indexOf(".bounds(cardX + 120, cardY + 270, 20, 20).build());", start); if (start != -1 && end != -1) { end += ".bounds(cardX + 120, cardY + 270, 20, 20).build());".length(); String newBtn = "importButton = this.addRenderableWidget(Button.builder(\n            net.minecraft.network.chat.Component.literal(\"\uD83D\uDCC1\"),\n            button -> {\n                boolean isAndroid = System.getProperty(\"os.name\").toLowerCase().contains(\"android\") || System.getProperty(\"java.vm.name\").toLowerCase().contains(\"dalvik\") || System.getProperty(\"java.vm.name\").toLowerCase().contains(\"art\");\n                if (isAndroid) {\n                    String p = com.luminabox.audio.CustomMusicManager.getInstance().getLocalMusicFolder().getAbsolutePath();\n                    com.luminabox.audio.CustomMusicManager.showToast(\"Android: Put music in \" + p);\n                } else {\n                    org.lwjgl.system.MemoryStack stack = org.lwjgl.system.MemoryStack.stackPush();\n                    try {\n                        org.lwjgl.PointerBuffer filters = stack.mallocPointer(4);\n                        filters.put(stack.UTF8(\"*.mp3\"));\n                        filters.put(stack.UTF8(\"*.wav\"));\n                        filters.put(stack.UTF8(\"*.ogg\"));\n                        filters.put(stack.UTF8(\"*.flac\"));\n                        filters.flip();\n                        String selectedFiles = org.lwjgl.util.tinyfd.TinyFileDialogs.tinyfd_openFileDialog(\n                            \"Import Music Files\",\n                            System.getProperty(\"user.home\") + java.io.File.separator,\n                            filters,\n                            \"Audio Files\",\n                            true\n                        );\n                        if (selectedFiles != null && !selectedFiles.isEmpty()) {\n                            String[] files = selectedFiles.split(\"\\\\|\");\n                            java.io.File destDir = com.luminabox.audio.CustomMusicManager.getInstance().getLocalMusicFolder();\n                            for (String fPath : files) {\n                                java.io.File src = new java.io.File(fPath);\n                                if (src.exists() && src.isFile()) {\n                                    java.io.File dest = new java.io.File(destDir, src.getName());\n                                    java.nio.file.Files.copy(src.toPath(), dest.toPath(), java.nio.file.StandardCopyOption.REPLACE_EXISTING);\n                                }\n                            }\n                        }\n                    } catch (Exception e) {\n                        com.luminabox.LuminaBox.LOGGER.error(\"Failed to import files\", e);\n                    } finally {\n                        stack.close();\n                    }\n                }\n            }\n        ).bounds(cardX + 120, cardY + 270, 20, 20).build());"; content = content.substring(0, start) + newBtn + content.substring(end); } Files.write(Paths.get(path), content.getBytes(\"UTF-8\")); } }
